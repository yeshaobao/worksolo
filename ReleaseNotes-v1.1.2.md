# WorkSolo v1.1.2

`v1.1.2` 是一次事项清单稳定性修复版本，重点解决增删改事项后列表显示不完整的问题。

## 本版修复

- 修复进入事项清单后，新建、删除或编辑事项会导致很多事项不再显示的问题。
- 修复项目筛选下拉框在列表刷新时可能把“全部项目”误判成“未分类”的问题。
- 将项目筛选内部值改为稳定字符串，明确区分“全部项目”“未分类”和具体项目，避免 WinUI `ComboBox` 刷新选项时产生歧义。

## 保持不变

- 事项主状态仍保持 `未开始`、`进行中`、`已完成`、`已取消`。
- 进展记录仍按天唯一，同一天再次保存会更新当天记录。
- 事项编辑仍使用独立窗口，`保存`、`保存并关闭`、`关闭` 行为不变。
- 数据仍保存在 `%LOCALAPPDATA%\WorkSolo\data.json`，升级不需要迁移已有数据。

## 升级建议

如果你在 `v1.1.1` 中遇到事项清单刷新后列表变少、只剩一部分事项的问题，建议直接升级到本版本。
